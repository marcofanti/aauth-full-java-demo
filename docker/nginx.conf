server {
    listen 3050;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback: /auth-callback (the consent popup landing page) must serve the app.
    location / {
        try_files $uri /index.html;
    }
}
